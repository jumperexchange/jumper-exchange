{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^5.1.0"
  },
  "scripts": {
    "bootstrap": "yarn install; lerna bootstrap;",
    "start": "lerna run start --parallel",
    "storybook": "lerna run storybook"
  },
  "workspaces": [
    "packages/*"
  ]
}
