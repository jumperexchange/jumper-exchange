apiVersion: v1
kind: Service
metadata:
  name: {{ include "tracking-gateway.fullname" . }}
  labels:
    {{- include "tracking-gateway.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tracking-gateway.selectorLabels" . | nindent 4 }}
