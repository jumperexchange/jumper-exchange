apiVersion: v1
kind: Service
metadata:
  name: {{ include "tracking-integrations.fullname" . }}
  labels:
    {{- include "tracking-integrations.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tracking-integrations.selectorLabels" . | nindent 4 }}
