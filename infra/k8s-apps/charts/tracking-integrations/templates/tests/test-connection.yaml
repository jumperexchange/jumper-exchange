apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "tracking-integrations.fullname" . }}-test-connection"
  labels:
    {{- include "tracking-integrations.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "tracking-integrations.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
