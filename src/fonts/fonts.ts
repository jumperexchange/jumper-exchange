import { Inter, Urbanist } from 'next/font/google';

export const inter = Inter({
  subsets: ['latin'],
  display: 'swap',
});

export const urbanist = Urbanist({
  subsets: ['latin'],
  display: 'swap',
});
