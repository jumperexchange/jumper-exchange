/**
 * A component that renders an empty state. Mainly needed for conditional translation rendering.
 * @returns A component that renders an empty state
 */

export const EmptyComponent = () => {
  return <></>;
};
