import { Box, styled } from '@mui/material';

export const BackButtonMainBox = styled(Box)(({ theme }) => ({
  width: '80%',
  maxWidth: '1210px',
  display: 'flex',
  flexDirection: 'row',
  justifyContent: 'left',
  alignContent: 'center',
  alignItems: 'center',
  marginBottom: '16px',
}));
