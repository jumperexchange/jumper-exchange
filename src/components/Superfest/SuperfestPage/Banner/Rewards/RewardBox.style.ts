import { Box, styled } from '@mui/material';

export const RewardTopBox = styled(Box)(() => ({
  display: 'flex',
  flexDirection: 'column',
  alignContent: 'flex-start',
  justifyContent: 'flex-start',
}));

export const RewardSubtitleBox = styled(Box)(() => ({
  display: 'flex',
  textAlign: 'left',
  alignContent: 'flex-start',
  justifyContent: 'flex-start',
}));
