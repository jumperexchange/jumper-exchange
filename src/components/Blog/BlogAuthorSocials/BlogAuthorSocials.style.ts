import { Box, styled } from '@mui/material';

export const BlogAuthorSocialsContainer = styled(Box)(({ theme }) => ({
  marginTop: theme.spacing(0.5),
  display: 'flex',
  alignItems: 'flex-start',
}));
