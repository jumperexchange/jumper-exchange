import { ChainId } from '@lifi/sdk';

export const ZAP_ALLOWED_SOURCE_CHAINS = [
  ChainId.ETH,
  ChainId.BAS,
  ChainId.ARB,
  ChainId.PLA,
  ChainId.KAT,
  ChainId.HYP,
  ChainId.LNA,
  ChainId.OPT,
];
