export const INTERCOM_USER_ID_V2_COOKIE = 'jumper_intercom_user_id_v2';

/** Shared lifetime for the v2 user id cookie and the signed Intercom JWT (seconds). */
export const INTERCOM_SESSION_MAX_AGE_SECONDS = 60 * 60 * 24 * 365;
