import balances from './index'

describe.skip('balances', () => {
  // TODO we need to investigate how we can properly test provider and contract calls
  // integration tests are also possible

  it('should work', () => {})
})
